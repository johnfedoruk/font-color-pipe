import { FontColorPipePipe } from './font-color-pipe.pipe';

describe('FontColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FontColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
